Setup and run
--------------

1. Create a virtual environment and activate it (Windows):

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python demo_scenario.py
```

2. For quick iteration, edit files and re-run the demo.
